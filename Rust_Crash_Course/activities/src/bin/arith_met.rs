fn main(){
    let first = 5;
    let second = 10; // don't try working with float and int, errors out
    let fs_sum = first + second;
    println!("{:?}", fs_sum);
    let third = 3;
    let mul = first * third; 
    println!("Multiply {} with {}: {}", first, third, mul);
}