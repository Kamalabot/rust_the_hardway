#![allow(unused_imports)]
#![allow(warnings)]

use iterators::*;
mod prac_mod0;
use prac_mod0::prac;

fn main() {
    // basic_iterator();
    // cons_iterator();
    // collect_filter();
    // enumerati();
    // any_all();
    // paired();
    // let v = &[1, 2, 3, 4, 5];
    // let p = &[10, 20, 35, 45, 52];
    // let pv = vec![vec![10, 20], vec![35, 45], vec![52, 26]];
    // find_ex(v);
    // chain_flat(v, p, pv);
    // custom_iterator_example();
    // complex_chaining_example();
    prac()
}
